# M004 S03 Plan: Real Inbox UAT & Release Gate

## Goal

在真实微信会话和真实收件箱中验证自然语言邮件路径，并决定是否将 `v1.4.1` 作为可发布口径。

## Must-haves

- 在真实微信会话中验证自然语言纯文本邮件
- 在真实收件箱中验证 `mail.defaultTo` fallback
- 明确当前自然语言邮件的范围限制

## Exit

- UAT 通过后，可将 `M004` 标记为 release ready
